import React from 'react';
import cl from './Home.module.scss';

interface Props {

}

const Home = (props: Props) => {
    return (
        <div className={cl.home}>
            Home
        </div>
    );
};

export default Home;